# Schemathesis Contract Testing — Petstore API

This project uses Schemathesis to run property-based contract tests against the public Petstore Swagger API.

## Quick Start

```bash
pip install -r requirements.txt
pytest contract_tests/
